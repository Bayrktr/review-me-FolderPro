

/*
class AlphabetDropDownButton extends StatelessWidget {
  const AlphabetDropDownButton({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () {},
      icon: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          const Text(
            'A - Z',
          ),
          Padding(padding: context.padding.veryLow),
          Icon(
            Icons.arrow_drop_down,
            color: context.theme.getColor.iconColor,
            size: context.radius.low,
          ),
        ],
      ),
    );
  }
}

 */
